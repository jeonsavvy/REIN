# Verification Evidence Ledger

Last updated: 2026-07-21 KST

## Local deterministic verification

- Required-order run after the REIN and x402-origin fixes:
  1. `pnpm lint`: passed.
  2. `pnpm typecheck`: passed.
  3. `pnpm test`: 9 files, 48 tests passed.
  4. `pnpm build`: Next.js production build passed; all nine routes compiled.
  5. `pnpm test:e2e`: 8 Playwright cases passed across desktop and Pixel 7.
- Desktop/mobile screenshots were rendered and visually inspected for overlap,
  clipping, overflow, hierarchy, contrast, and unintended crop.
- Source-only UX audit: `PASS`.
- Demo receipts remain labelled `SIMULATED` and never expose Explorer links.
- The Dockerfile was built by Cloud Build from the final source and deployed.
- `pnpm preflight:live` rejects malformed wallet-key input and never prints the
  secret value.
- Source-text credential scan found no private-key block or Google API-key
  pattern; `.env.example` contains placeholders only.

## Live GCP verification

- Project: isolated hackathon project `rein-solana-agent-2026`.
- Cloud Run: <https://rein-vvwpcipqca-du.a.run.app>.
- Ready revision: `rein-00005-wh5`, 100% traffic.
- Health: service `rein`, mode `live`, storage `firestore`.
- Scaling: service max `2`, revision max `2`, min defaults to `0`.
- Runtime identity has only Vertex user, Datastore user, and resource-scoped
  buyer-secret accessor permissions needed by the app.
- Secret mapping references `rein-svm-private-key:latest`; no secret value was
  printed or written to the repository.
- Billing notification: monthly ₩35,000 with 20%, 40%, 100% thresholds. This is
  an alert, not a spend blocker.
- Vertex ADK feasibility call used `gemini-3.5-flash` in `global`, returned the
  two expected catalog selections, and exposed no thoughts.
- The installed ADK `1.4.0` Interactions adapter did not forward structured
  output/thinking configuration; the verified GenerateContent path does.

## Live Solana/x402 verification

- Network: `solana:EtWTRABZaYq6iMfeYKouRu166VU2xqa1`.
- Asset: Circle Devnet USDC
  `4zMMC9srt5Ri5X14GAgXhaHii3GnPAEERYPJgZJDncDU`.
- x402 challenge was decoded before signing: public resource origin, exact
  scheme, fixed network/mint/payee/amount, and facilitator fee payer all matched.
- Verified run: `run_6f0e409bf94844558c6c7ba9f4ab9a1b`.
- Firestore result: `completed`, `spentAtomic=3000`, `reservedAtomic=0`, report
  generated by `Gemini 3.5 Flash` with four findings.
- Market payment, 1,000 atomic, `finalized`, no chain error:
  <https://explorer.solana.com/tx/2NuicT57mQD1Uu5yumPnubCkrdSVHQUegbxLEBsDtpdVTTjw5dTdyB3QpH9t7VZLGnyQyNV9DySA9xWMY9YMpArw?cluster=devnet>
- GitHub payment, 2,000 atomic, `finalized`, no chain error:
  <https://explorer.solana.com/tx/3vpyu3DsDvDT2m71kj3Pt5GQ4Ba2jQVYkuFhWL9eTUgiVXwpMiQKpbtjSPKaV5J5K3cpff6726kXT8p5Ui6gbcGR?cluster=devnet>
- Balance delta matched exactly: buyer `20.000000 -> 19.997000`, receiver
  `20.000000 -> 20.003000` test USDC.
- A first smoke run failed before settlement because the new receiver lacked an
  associated token account and the x402 resource URL used Cloud Run's internal
  origin. The reservation was released, no asset moved, both defects were fixed,
  and a new payment ID was used for the successful run.

## Submission artifact verification

- REIN deck source generated an eight-slide 16:9 PPTX and PDF. All eight final
  PDF pages were rendered to pixels and inspected for clipping, overlap, font
  fallback, hierarchy, contrast, margins, and unintended crop.
- Final PDF: `output/pdf/REIN-Hackathon-Deck.pdf` (964,863 bytes).
- A separate recording run, `run_7e8bcc6762404d8fa7ac7b1afc592201`,
  completed once without retry and spent exactly 3,000 atomic test USDC.
- Recording market payment, 1,000 atomic, `finalized`, `err: null`:
  <https://explorer.solana.com/tx/cJW9o6c4X5Wh8YkXDBySbYdBb7y2BLdtrSc7A6qt6JDTQaTabqDDaekyfqCYUSbtAkJD8Sq3MTQUcvviRGxMMHm?cluster=devnet>
- Recording GitHub payment, 2,000 atomic, `finalized`, `err: null`:
  <https://explorer.solana.com/tx/2zpggYuHJ7Z5KCbg4iXqeTryhcevA5mqmHx1G57BZ8U73XxgaRQho2bgmg6rXnrzNBJhCBGoEn7QstGrLqXUAeeQ?cluster=devnet>
- Recording-run balances matched exactly: buyer `19.997 -> 19.994`, receiver
  `20.003 -> 20.006` test USDC.
- Final video: `output/video/edit/REIN-demo-final.mp4`, 100.47 seconds,
  1920x1080 at 30 fps, H.264 video, 48 kHz AAC mono, 7,492,890 bytes.
- Video SHA-256:
  `C0FFCBE999811E6B8A596513D0C5BF3DE69D7A6441B18EA58D8C0DB80719B271`.
- Video frames across intro, purchase, report, both Explorer pages, and closing
  card were rendered and inspected. Captions did not clip or cover transaction
  status fields. Audio measured `mean_volume=-16.8 dB`, `max_volume=-1.5 dB`.
- Local faster-whisper reverse transcription recognized the key `HTTP 402`,
  amounts, report flow, finalized Explorer proof, and no-retry behavior. The
  closing caption discloses AI-generated narration.

## Spec implementation review

- Outcome: aligned; no missing public API, policy, persistence, failure-state,
  live-proof, PDF, or video acceptance criterion was found.
- Intentional safety refinement: Gemini performs two structured ADK calls and
  receives no purchase tools. The deterministic orchestrator owns catalog,
  policy, reservation, and payment, so the planned six-call ceiling is
  vacuously satisfied with zero model-controlled tool calls.
- The three-hour fallback was not activated because the official x402 path
  settled within the gate and produced finalized Devnet evidence.
- Known residual risk: the judge-facing no-login endpoint can consume test
  quota and Vertex requests. Controls are daily atomic quota, Cloud Run max 2,
  billing alerts, Devnet-only funds, and service/secret shutdown after judging;
  billing alerts do not block spend.

## Public repository reproduction

- Public repository: <https://github.com/jeonsavvy/REIN>, default branch `main`.
- Published root commit:
  `925472154e77cc2bfb268d6187c2847944f34d9f`.
- A new `--depth 1` clone from GitHub successfully ran
  `pnpm install --frozen-lockfile`, then `pnpm lint`, `pnpm typecheck`,
  `pnpm test` (48/48), `pnpm build` (nine routes), and `pnpm test:e2e` (8/8).
- Repository metadata points to the verified Cloud Run URL and uses the
  `ai-agents`, `cloud-run`, `gemini`, `solana`, `vertex-ai`, and `x402` topics.

## Remaining submission evidence

- Public YouTube URL and public Drive PDF URL.
- User-owned identity/contact fields, terms acceptance, and Google Form submit.

No remaining item is represented as complete in the form draft.
